{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Listen for XDebug",
            "type": "php",
            "request": "launch",
            "port": 80,
            "pathMappings": {
                "/app": "${workspaceRoot}"
            }
        }
    ]
}
